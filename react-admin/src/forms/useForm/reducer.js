export const SET_FORM_DATA = 'SET_FORM_DATA';

export const initialState = {
  formData: {},
  errors: {},
};

export const reducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
