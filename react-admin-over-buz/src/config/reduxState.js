const namespace = '@newtash/app-core';
export const appNamespace = '@newtash/app';

const reduxState = {
  APP: `${namespace}/app`,
  AUTH: `${namespace}/auth`,
  WHO_AM_I: `${namespace}/whoAmI`,
  ERRORS: `${namespace}/errors`,
};

export default reduxState;
